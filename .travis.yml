branches:
  only:
    - pre-prod
language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm run build
  - npm run test
# deploy:
#   provider: pages
#   skip_cleanup: true
#   github_token: $github_token
#   local_dir: build
#   on:
#     branch: sensei
after_script:
- gulp deploy --host $FTP_HOST --user $FTP_USER --password $FTP_PASSWORD